[package]
name = "shared"
version = "0.1.0"
edition = "2021"

[lib]
name = "shared"
path = "src/lib.rs"

[dependencies]
serde = { version = "1.0.195", features = ["derive"] }
serde_json = "1.0.111"

complex = { path = "../complex" }

